The "Processors" folder *must* be added to the system environmental variable "PYTHONPATH".
It contains other subfolders, each representing a different geoprocessor as importable python module (thanks to the __init__.py, search for it for more info).

Each processor module, has a config file, which [.... ADD INFO ON THE PROCESSORS]